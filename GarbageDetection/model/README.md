# ML models for on-device inference

Preferred file (already used by `config.py`):

- `rls_mobilenetv3_mish_cbam_float16.tflite`

Fallback name:

- `model.tflite`

Source training repo: https://github.com/GratifyPVT/ML-model
