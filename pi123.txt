# fix the overwrite conflict
sudo apt install -y -o Dpkg::Options::="--force-overwrite" raspberrypi-ui-mods

# finish any half-installed packages
sudo dpkg --configure -a
sudo apt -f install -y

# install login manager if needed
sudo apt install -y lightdm

# set graphical boot
ls /lib/systemd/system/graphical.target
sudo systemctl set-default graphical.target




sudo raspi-config
# System Options → Boot / Auto Login → Desktop Autologin

sudo systemctl enable gratify-ads
sudo reboot