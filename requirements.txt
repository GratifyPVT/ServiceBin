# Core packages only (ads + waste + camera helpers).
# Safe on Raspberry Pi OS Trixie / Python 3.13 / armv7l.
requests
numpy
opencv-python-headless
pyserial

# ML: install tflite-runtime ONLY inside Python 3.9–3.11 (e.g. conda env mf).
# Do NOT uncomment on system Python 3.13 — pip will fail and block everything:
# tflite-runtime
