# Core packages
requests
numpy<2
pyserial
Pillow

# Do NOT install opencv-python-headless on Pi OS Trixie
# (FFmpeg ABI mismatch). ML camera uses system ffmpeg.

# ML venv (Python 3.11) also needs:
#   pip install tflite-runtime==2.14.0
