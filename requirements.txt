# Core packages (ads + waste work on system Python 3.13 venv too)
requests
numpy
pyserial
Pillow

# Do NOT install opencv-python-headless on Pi OS Trixie —
# pip wheels need old FFmpeg (LIBSWSCALE_6) and break with soname hacks.
# ML camera uses system ffmpeg instead (see GarbageDetection/runtime.py).

# ML only (Python 3.11 venv ~/gratify-ml-venv):
#   pip install tflite-runtime==2.14.0
